use crate::prelude::*;

pub fn player_fov(query: Query<&FieldOfView, With<Player>>) -> () {
    // pub fn player_fov<'a>(query: Query<&'a FieldOfView, With<Player>>) -> &'a FieldOfView {
    // query.iter().nth(0).unwrap()
}
